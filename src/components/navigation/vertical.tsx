import { Route } from './routes';
import { FC } from 'react';

type Props = {
  config: Route[];
};

const VerticalNavigation: FC<Props> = ({ config }) => {
  return <></>;
};

export default VerticalNavigation;
