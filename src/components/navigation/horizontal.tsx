import { Route } from './routes';
import { FC } from 'react';

type Props = {
  config: Route[];
};

const HorizontalNavigation: FC<Props> = ({ config }) => {
  return <></>;
};

export default HorizontalNavigation;
